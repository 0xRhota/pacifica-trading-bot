⚠️  SECURITY WARNING ⚠️

The .env file in this directory contains your PRIVATE WALLET KEY.

NEVER:
- Commit .env to git
- Share .env with anyone
- Post screenshots showing .env contents
- Store .env in cloud sync folders unencrypted

The .env file is already in .gitignore and will NOT be committed.

To verify:
  git check-ignore -v .env

Should show:
  .gitignore:2:.env    .env

If you accidentally commit .env:
1. IMMEDIATELY rotate your wallet key
2. Move funds to new wallet
3. git rm --cached .env
4. git commit --amend

This file (.env.README) is safe to commit as it contains no secrets.
